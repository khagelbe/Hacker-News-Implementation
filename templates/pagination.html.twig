<ul class="pagination">
	{% for i in range(1, maxPages) %}
		{% if i == page %}
			<li class="active"><a href="{{ url(pageName, {page: i}) }}">{{ i }}</a></li>
		{% else %}
			<li><a href="{{ url(pageName, {page: i}) }}">{{ i }}</a></li>
		{% endif %}
	{% endfor %}
</ul>
